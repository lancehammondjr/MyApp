import React from 'react';
const bootstrap = require('bootstrap')

const Home = (props: { name: string }) => {

    return (
        <div>
            

        </div>
    );

};

export default Home;